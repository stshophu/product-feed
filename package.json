{
  "name": "shopify-wsnl-sync",
  "version": "2.0.0",
  "description": "Shopify → Winkelstraat.nl catalog sync with location-based markup",
  "main": "sync.js",
  "scripts": {
    "sync": "node sync.js",
    "full": "node sync.js --full"
  },
  "dependencies": {
    "axios": "^1.7.0",
    "dotenv": "^16.4.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
